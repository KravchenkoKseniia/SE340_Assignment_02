# AFTER query rewrites + indexes

Order followed: rewrite the query first, then add the matching index

Indexes in place for these queries:
- `idx_customers_email` - GIN trigram on `customers(email)` (for `LIKE '%...%'`)
- `idx_orders_status` - b-tree on `orders(status)`
- `idx_events_customer_time` - composite on `customer_events_wide(customer_id, event_time)`
- dropped the redundant `idx_events_customer_id`

## Before / After summary

| # | Query | Before | After (rewrite + index) |
|---|-------|-------:|------------------------:|
| Q1 | search_customer_by_email | 2.12 ms · 333 buf | 0.039 ms · 7 buf |
| Q2 | orders_by_city_and_status | 12.20 ms · 1,225 buf | 7.68 ms · 1,253 buf |
| Q6 | cartesian_pressure | 58.12 ms · 10,872 buf | 38.66 ms · 1,805 buf (rewrite + index + VACUUM, on normalized table) |

---

## Q1 - rewrite (no `SELECT *`) + trigram index 

```text
Bitmap Heap Scan on customers  (actual time=0.022..0.022 rows=0 loops=1)
  Recheck Cond: (email ~~ '%gmail%')
  Buffers: shared hit=7
  ->  Bitmap Index Scan on idx_customers_email  (rows=0)
        Index Cond: (email ~~ '%gmail%')
Execution Time: 0.039 ms
```

## Q2 - rewrite (drop `LIKE '%a%'` + `SELECT *`) + general index 

```text
Bitmap Heap Scan on orders  (actual time=0.765..6.940 rows=25077 loops=1)
  Recheck Cond: (status = 'paid')
  Heap Blocks: exact=1230
  Buffers: shared hit=1230 read=23
  ->  Bitmap Index Scan on idx_orders_status  (rows=25077)
        Index Cond: (status = 'paid')
Execution Time: 7.68 ms
```

## Q6 - rewrite (`EXISTS`) + composite index

### BEFORE VACUUM

```text
Aggregate  (actual time=57.084..57.087 rows=1 loops=1)
  Buffers: shared hit=50614 read=767
  ->  Nested Loop Semi Join  (rows=11998)
        ->  Hash Join (customers x distinct orders.customer_id)  (rows=13176)  Buffers: hit=1566
        ->  Index Only Scan using idx_events_customer_time on customer_events_wide e  (loops=13176)
              Index Cond: (customer_id = o.customer_id AND event_time >= now() - '90 days')
              Heap Fetches: 10233
              Buffers: shared hit=49048 read=767
Execution Time: 57.16 ms
```
What we have here
- The triple JOIN became a `Nested Loop Semi Join`
- The `EXISTS` now uses an Index Only Scan (`event_time` is inside the composite index)
- `Heap Fetches: 10233` -> the Index Only Scan still had to hit the heap

### `VACUUM (ANALYZE) customer_events_wide`

```text
Aggregate  (actual time=53.223..53.226 rows=1 loops=1)
  Buffers: shared hit=2339
  ->  Hash Join  (Hash Cond: o.customer_id = c.customer_id)  (rows=11996)
        ->  HashAggregate (distinct orders.customer_id)  (rows=19956)  Buffers: hit=1230
        ->  Hash  (rows=12024)
              ->  Hash Join  (c.status IN (...) x distinct events.customer_id)  (rows=12024)
                    ->  Seq Scan on customers c  Filter: status = ANY('{active,inactive}')
                    ->  HashAggregate (distinct events.customer_id)  (rows=18195)
                          ->  Index Only Scan using idx_events_customer_time on customer_events_wide e
                                Index Cond: (event_time >= now() - '90 days')
                                Heap Fetches: 0
                                Buffers: shared hit=773
Execution Time: 53.45 ms
```

### Pointed at the normalized `customer_events` (narrow table)

After Q4's normalization split, Q6's events `EXISTS` reads the narrow
`customer_events` instead of `customer_events_wide`. Same plan shape, but the
covering index is smaller so the events side gets cheaper:

```text
Aggregate  (actual time=38.554..38.556 rows=1 loops=1)
  Buffers: shared hit=1567 read=238
  ->  Hash Join  (Hash Cond: o.customer_id = c.customer_id)  (rows=11996)
        ->  HashAggregate (distinct orders.customer_id)  (rows=19956)  Buffers: hit=1230
        ->  Hash  (rows=12024)
              ->  Hash Join  (c.status IN (...) x distinct events.customer_id)  (rows=12024)
                    ->  Seq Scan on customers c  Filter: status = ANY('{active,inactive}')
                    ->  HashAggregate (distinct events.customer_id)  (rows=18196)
                          ->  Index Only Scan using idx_events_time_cust_type on customer_events e
                                Index Cond: (event_time >= now() - '90 days')
                                Heap Fetches: 0
                                Buffers: shared hit=1 read=238
Execution Time: 38.66 ms
```

---
This md file was generated by Claude Code from the EXPLAIN output to make it more readable and easier to view :D